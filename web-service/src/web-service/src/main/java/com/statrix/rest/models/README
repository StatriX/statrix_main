1) id в конструкторах - не нужны, пользователь их создать не сможет, веб сервис тоже,
    единственный кто может - база, т.к. это автоинкремент, и база в ответе будет возвращать id.

2) Обратите внимание, что у моделей могут быть не все getter-ы или setter-ы,
    потому что некоторые поля в них не нуждаются.

3) модели: DbChargings, DbPages - не нужны

4) Keywords переделать